{
  "buildCommand": "python3 manage.py collectstatic --noinput && vercel-build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ],
  "functions": {
    "api/wsgi.py": {
      "maxDuration": 15
    }
  }
}