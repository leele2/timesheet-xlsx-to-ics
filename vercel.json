{
  "functions": {
    "api/wsgi.py": {
      "maxDuration": 15
    }
  },
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "python3 manage.py collectstatic --noinput",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ]
}